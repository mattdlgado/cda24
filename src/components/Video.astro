<video
  autoplay
  loop
  muted
  playsinline
  class="fixed -z-50 top-0 left-0 w-full h-full object-cover"
>
  <source src="/background-video.mp4" type="video/mp4" />
</video>

<style>
  video {
    opacity: 0;
    animation: fadeIn 1000ms ease-in-out 250ms forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }
</style>
